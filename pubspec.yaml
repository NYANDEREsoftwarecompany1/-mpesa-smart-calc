name: mpesa_smart_calc
description: M-PESA Smart Calc KE - Calculate fees before you send. Avoid surprises.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # ADS - REAL MONEY
  google_mobile_ads: ^5.1.0

  # OPEN *334# + PRIVACY POLICY LINK
  url_launcher: ^6.2.5

  # SAVE HISTORY
  shared_preferences: ^2.2.2

  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # ASSETS - KAMA UKO NAZO
  # assets:
  #   - assets/images/
